import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Study Master")
        Button("Change Background Color"){
            
            
            
        }
        .toggleStyle(.switch)
    }
}
